1. Display current date on top of screen
2. Display time blocks from 9-5 (8 hours)
3. Change time blocks color based on past, present, and future
  a. Past = Grey
  b. Present = Red
  c. Future = Green 
4. 





1. Create variables to store references to html elements 
    a. #currentDay 
    b. #hour-(9-5)
    c. .saveBtn
2. Add an event listener to listen for save button
    a. When a user clicks save THEN save to .description
3. 





// Created a variable that will store the current date formatted ddd.MMM.DD
var current = dayjs().format("dddd, MMM D");
// Created a variable that will store the current hour
var currentHours = dayjs().hour();
var endOfWorkDay = dayjs().add(8, "hour");